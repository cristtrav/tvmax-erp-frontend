import { OpenLocationCodePipe } from './open-location-code.pipe';

describe('OpenLocationCodePipe', () => {
  it('create an instance', () => {
    const pipe = new OpenLocationCodePipe();
    expect(pipe).toBeTruthy();
  });
});
