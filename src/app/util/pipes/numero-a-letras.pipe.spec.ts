import { NumeroALetrasPipe } from './numero-a-letras.pipe';

describe('NumeroALetrasPipe', () => {
  it('create an instance', () => {
    const pipe = new NumeroALetrasPipe();
    expect(pipe).toBeTruthy();
  });
});
